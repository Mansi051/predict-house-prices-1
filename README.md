# predict-house-prices-1
This project implements a single linear regression model to predict house prices based on the size of the house. The model learns a linear relationship between house size (input feature) and price (output) using gradient descent to minimize prediction error.
